# Vite build-time API URL (required for Vercel production)
# Local dev: leave unset — vite.config.js proxies /api to localhost:8001
# Vercel: set in Project → Settings → Environment Variables
VITE_API_BASE=https://YOUR_RENDER_SERVICE.onrender.com
